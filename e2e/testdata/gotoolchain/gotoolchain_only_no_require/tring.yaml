version: 1

groups:
  - name: go-only
    description: Test that track_require false doesn't update require deps
    resolver: gotoolchain
    resolver_config:
      base_url: "{{GOTOOLCHAIN_URL}}"
    sources:
      - type: gomod
        config:
          manifest_paths:
            - go.mod
          track_require: false
          track_go: true
          track_toolchain: false
    selectors:
      include:
        module_patterns:
          - "*"
    policy:
      selection:
        strategy: patch
